[package]
name = "rocks2d_physics"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.9.2"
raylib = { version = "5.5.1", features = ["wayland"] }

[dev-dependencies]
criterion = { version = "0.8.1", features = ["html_reports"] }

[[bench]]
name = "pendulumn"
harness = false